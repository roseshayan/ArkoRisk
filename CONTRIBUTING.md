# مشارکت در ArkoRisk

از گزارش باگ، پیشنهاد قابلیت و Pull Request استقبال می‌شود.

## قبل از ارسال تغییر

1. Issue مرتبط را بررسی یا یک Issue جدید با سناریوی بازتولید ایجاد کنید.
2. تغییر را تا حد ممکن کوچک و مستقل نگه دارید.
3. کد را با `#property strict` در آخرین MetaEditor 5 کامپایل کنید.
4. نتیجه Compile، نماد، نوع حساب (Netting/Hedging)، بروکر و تایم‌فریم تست را ثبت کنید.
5. تغییرات مربوط به ارسال سفارش را ابتدا روی حساب دمو تست کنید.

## قواعد کد

- نام‌گذاری موجود و پیشوندهای `RP_` و `ArkoRisk` را حفظ کنید.
- از ارسال تکراری سفارش در حلقه یا Retry نامحدود خودداری کنید.
- هیچ API Key، شماره حساب، لاگ شخصی یا اسکرین‌شات خصوصی Commit نکنید.
- برای قابلیت جدید، Input قابل غیرفعال‌سازی و توضیح مستند اضافه کنید.
- رفتار حساب‌های Netting و Hedging را جداگانه در نظر بگیرید.

## Pull Request

در توضیح PR بنویسید چه مشکلی حل شده، چگونه تست شده و آیا روی ریسک یا ارسال سفارش اثر دارد. تغییرات مالی حساس بدون شواهد تست دمو Merge نمی‌شوند.

